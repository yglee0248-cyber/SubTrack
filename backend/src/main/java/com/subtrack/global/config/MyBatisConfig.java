package com.subtrack.global.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MyBatisConfig {

    // Dao interfaces will be added in later domain tasks.
    // Mapper scanning can be enabled here when those interfaces exist.
}
